[pytest]
# Make the project root importable so `import app...` works whether tests are
# launched via `pytest` or `python -m pytest` (bare `pytest` does NOT add the
# cwd to sys.path by default).
pythonpath = .
testpaths = tests
